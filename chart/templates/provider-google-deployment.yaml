apiVersion: apps/v1
kind: Deployment
metadata:
  name: provider-google
  labels:
    app: pipem.io
    component: provider-google
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      component: provider-google
  template:
    metadata:
      labels:
        component: provider-google
    spec:
      containers:
        - name: provider-google
          image: {{ .Values.image }}:{{ .Values.version }}
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              memory: "32Mi"
              cpu: "10m"
            limits:
              memory: "256Mi"
              cpu: "100m"
          envFrom:
            - configMapRef:
                name: pipem-config
          env:
            - name: 'MONGODB_URI'
              valueFrom:
                secretKeyRef:
                  name: pipem-secret
                  key: MONGODB_URI
            - name: "GOOGLE_TRIGGER_URL"
              value: "https://console.cloud.google.com/cloud-build/triggers/edit/#TRIGGERID?project=#PROJECTID"
